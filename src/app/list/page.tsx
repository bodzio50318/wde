import CreateList from "./CreateListButton";

export default async function Lists() {
    return (
        <html lang="en">
            <body>
                <h1>Lists</h1>
                <CreateList/>
            </body>
        </html>
    );
}